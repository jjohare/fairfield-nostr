# Nostr Configuration
PUBLIC_DEFAULT_RELAYS=wss://relay.damus.io,wss://relay.nostr.band,wss://nos.lol

# AI Configuration (Optional)
PUBLIC_AI_ENABLED=false
AI_API_KEY=your_api_key_here
AI_API_URL=https://api.example.com

# Application Configuration
PUBLIC_APP_NAME=Fairfield Nostr Chat
PUBLIC_APP_VERSION=0.1.0

# ===========================================
# SECURITY: Admin Configuration
# ===========================================
# ⚠️ CRITICAL: Admin configuration MUST be synchronized across:
#   1. This .env file (VITE_ADMIN_PUBKEY)
#   2. relay/whitelist.json (admins array)
#
# Source of Truth: relay/whitelist.json
# The relay whitelist.json file is the authoritative source for admin permissions.
# This environment variable should match the pubkeys in whitelist.json for consistency.
#
# Configuration Steps:
# 1. Generate a new key pair:
#    npx nostr-tools-cli generate
#
# 2. Convert your npub to hex format (64 characters):
#    npx nostr-tools-cli decode npub1...
#
# 3. Update BOTH locations:
#    a) Add the hex pubkey to relay/whitelist.json admins array
#    b) Set VITE_ADMIN_PUBKEY below with the same hex value
#
# 4. For multiple admins (comma-separated):
#    VITE_ADMIN_PUBKEY=pubkey1hex,pubkey2hex,pubkey3hex
#
# 5. SECURITY WARNINGS:
#    - DO NOT commit actual admin keys to version control
#    - Set this in your deployment .env file (not .env.example)
#    - Replace the placeholder "0000...0000" key in whitelist.json
#    - Rotate keys regularly
#
# Example:
# VITE_ADMIN_PUBKEY=55f6d852c8ecbf022be81be356b62fdeef09c900deaf2bd262dc6759427c2eb2
#
# Multiple admins:
# VITE_ADMIN_PUBKEY=55f6d852c8ecbf022be81be356b62fdeef09c900deaf2bd262dc6759427c2eb2,a7e2c8f1d9b3e4a5f6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9
VITE_ADMIN_PUBKEY=

# Enable NDK debug logging (optional - DO NOT enable in production)
VITE_NDK_DEBUG=false

# ===========================================
# CONTROLLER & BACKUP SERVICE CONFIGURATION
# ===========================================
# Controller HTTP server port
CONTROLLER_PORT=8080

# Relay URL for controller services
RELAY_URL=ws://relay:7777

# Whitelist file path
WHITELIST_PATH=./relay/whitelist.json

# Backup configuration
BACKUP_DIR=./backups
BACKUP_RETENTION=10
BACKUP_SCHEDULE=daily

# Backup encryption (optional - recommended for production)
# Generate a strong key: openssl rand -base64 32
BACKUP_ENCRYPTION_KEY=
